package com.william.core;

public final class Constants {
    private Constants(){};

    /** Root folder where all cat pics are persisted.
     * In the future we might want to extend functionality to allow
     * separate different kinds of cats into different subfolders under this root. */
    public static final String UPLOAD_FILE_ROOT = "cat_pic_uploads/";
}
